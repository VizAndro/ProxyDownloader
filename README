
DESCRIPTION
===============

Simple program to download global proxy pages and IP's. Only thies that can be connected within 5 sec. 
Really simple python 2.7 code.

HOW TO
===============

First to make the program work:
GlobalProxy.py pages  <- download pages with global proxy IP(ProxyPageList.txt). NEEDED to download the IP list.

GlobalProxy.py ip     <- download IP list to file ProxyList_1.txt


IF NOT WORKING
===============

-check chmod (eg. chmod a+x GlobalProxy.py)
